version: '2'
services:
    hello-docker:
        container_name: hello-docker
        build:
            context: hello-docker
            dockerfile: Dockerfile
        image: hello-docker
        ports:
            - "8080:8080"
